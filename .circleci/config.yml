version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:14.0.0
      - image: circleci/mysql:8.0.4
    steps:
      - checkout
      - run: npm install
      - run: node --version
      - run: sudo apt-get install default-mysql-client 
workflows:
  build_and_test:
    jobs:
      - build 
